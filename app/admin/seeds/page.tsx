import AdminConsole from '@/components/AdminConsole';
export const metadata = { title: 'Сиды | SwapSpot Admin', robots: { index: false, follow: false } };
export default function SeedsPage() { return <AdminConsole initialTab="seeds" />; }
